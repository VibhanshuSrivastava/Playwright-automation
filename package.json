{
  "name": "taskflow-app-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "TaskFlow: a React + Express task/project management demo app with a full Playwright end-to-end test suite (POM, API client, storage-state auth, visual regression).",
  "license": "MIT",
  "author": "",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "app",
    "app/backend",
    "playwright"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "npm run dev --prefix app",
    "dev:backend": "npm run dev --prefix app/backend",
    "dev:frontend": "npm run dev --prefix app",
    "dev:test": "npm run dev:test --prefix app",
    "build": "npm run build --prefix app",
    "lint": "npm run lint --prefix app && npm run lint --prefix playwright",
    "typecheck": "npm run typecheck --prefix app && npm run typecheck --prefix app/backend && npm run typecheck --prefix playwright",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test --prefix playwright",
    "test:headed": "npm run test:headed --prefix playwright",
    "test:headless": "npm run test:headless --prefix playwright",
    "test:ui": "npm run test:ui --prefix playwright",
    "test:report": "npm run report --prefix playwright"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.9.6"
  }
}
